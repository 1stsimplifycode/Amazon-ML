# 03 — Leakage Audit

Generated by `code/business_entity_resolution/src/leakage_audit.py`.

## 1. Repeated / near-duplicate S1 entities (train)

- exact duplicate (name, address, country): 0
- duplicate after cheap normalisation (name, address): 0
- S1 rows whose normalised **name** is shared with another S1: 866,894 (39.28%); largest group: `primary care group` × 253
  - top shared names: `primary care group` 253, `ear nose throat group` 251, `pediatric group` 222, `womens health group` 220, `physical therapy group` 218, `pediatric dental group` 216, `behavioral health group` 215, `chiropractic group` 209, `orthopedic group` 208, `eye group` 207, `dental group` 205, `family group` 204
- S1 rows whose normalised **address** is shared with another S1: 117,168 (5.31%); largest group ×14 `108 norle street college twp pa`
  - top shared addresses: `108 norle street college twp pa` 14, `104 roadrunner circle elephant butte nm` 13, `218 phillips street storm lake ia` 13, `43 franklin street delaware oh` 13, `55 lavkush vihar singhpur kachhar bithoor road kalyanpur kanpur nagar uttar pradesh` 13, `1921 willow park drive fort worth tx` 13, `43233 clearnight terrace loudoun county va` 12, `4804 gardenia avenue glendale az` 12
- singleton rate among S1 with a shared name: 5.59% vs unique name: 5.58%
- singleton rate among S1 with a shared address: 5.63% vs unique address: 5.58%

## 2. Duplicate S2/S3 records (train): do exact copies share a cluster?

| file | dup groups (name+addr+country) | records in groups | groups all same owner | groups all unmatched | groups mixed (different S1 or matched+unmatched) |
|---|---|---|---|---|---|
| train S2 | 25,060 | 50,933 | 25,060 | 0 | 0 |
| train S3 | 18,381 | 37,241 | 18,381 | 0 | 0 |

A *mixed* group means two byte-identical records have different labels. No model can separate those, which puts a hard ceiling on precision for them.

## 3. Identical text across sources

- train S2 records byte-identical (name, addr, country) to some S1: 1; share whose owner is that S1: 100.00%
- train S3 records byte-identical (name, addr, country) to some S1: 17; share whose owner is that S1: 100.00%

## 4. Collision families in the train pool (S2+S3, cheap-normalised)

- normalised names appearing with ≥5 distinct addresses: 125,690; with ≥5 distinct owners: 78,076
  - worst: `physical therapy` (1019 addrs, 859 owners), `primary care` (1013 addrs, 835 owners), `pediatric dental` (952 addrs, 792 owners), `urgent care` (931 addrs, 785 owners), `womens health` (931 addrs, 780 owners), `pediatric dentistry` (900 addrs, 766 owners), `behavioral health` (877 addrs, 755 owners), `internal medicine` (870 addrs, 741 owners)
- normalised addresses appearing with ≥5 distinct names: 12,418; with ≥3 distinct owners: 29,902
  - worst: `1 bangalore ka` (35 names, 34 owners), `no 1 chennai tn` (33 names, 31 owners), `no 5 bangalore ka` (32 names, 28 owners), `1 kolkata howrah wb` (31 names, 28 owners), `18 kolkata howrah wb` (32 names, 27 owners), `ground floor bangalore ka` (29 names, 25 owners)

## 5. Train ↔ test overlap

- source 1: shared entity_ids 0; byte-identical (name, addr, country) records train↔test: 0
- source 2: shared entity_ids 0; byte-identical (name, addr, country) records train↔test: 0
- source 3: shared entity_ids 0; byte-identical (name, addr, country) records train↔test: 0
- test S1 whose normalised name also occurs as a train S1 name: 34.51% (US 35.87%, India 44.44%, France 0.01%)

## 6. Validation design and why random pair splits are invalid

- Folds are assigned per **S1 entity** (hash of the S1 ID, 5 folds). All candidate pairs of an S1 (true and false) go to its fold.
- Pool records are exclusively owned (01: every S2/S3 is listed at most once), so a matched pool record can never be a positive in two folds.
- Distractor pool records (26%) are unlabelled negatives. They appear as negative candidates for S1 in several folds; that is not leakage, because the model never sees a label that ties them to a validation S1.
- A **random pair split** puts the positives of one S1 in both train and validation. The model then learns entity-specific name/address strings (`S1 name ↔ its own S2 variant`), and exclusivity/decision-layer features (rank of this pair within the S1, margin to the second candidate) are computed on information that crosses the split. It also cannot score entity-level F0.5, because a validation S1 has only part of its candidate set. Demonstration in the experiment ledger (EXP-LEAK).
- **Label-derived normalisation is leakage.** The learned Indic→Latin token dictionary uses GT pairs, so it is fit only on the fixed dictionary fold (fold 0). Folds 1–4 are used for CV and fold 0 is never scored. IDF/DF statistics are label-free and fit transductively on all records of the split being processed (train for CV, test for inference), which is legitimate.
- The threshold/decision layer is tuned on out-of-fold predictions only.

